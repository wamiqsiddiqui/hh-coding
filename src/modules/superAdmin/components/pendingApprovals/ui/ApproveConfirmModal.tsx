import { Dispatch, SetStateAction } from "react";

type ApproveConfirmModalProps = {
  setApproveConfirmModalOpen: Dispatch<SetStateAction<boolean>>;
};
const ApproveConfirmModal = ({}: //   setApproveConfirmModalOpen,
ApproveConfirmModalProps) => {
  return <div>ApproveConfirmModal</div>;
};

export default ApproveConfirmModal;
